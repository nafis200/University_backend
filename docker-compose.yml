version: '3.8'

services:
  backend:
    build: .
    container_name: my-backend-container
    ports:
      - "5000:5000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
    